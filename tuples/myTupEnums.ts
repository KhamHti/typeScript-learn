const user: (number | string)[] = [1, 2, 3, 4, '5'];
let tUser: (number | string | boolean)[]

tUser = [1, '2', true] 

tUser= [true, 2, 'string']

let rgb: [number, number, number] = [255,255,255];

type User = [number, string];

const newUser: User = [125, 'example@gg.com'];

newUser[1] = "of.com"
