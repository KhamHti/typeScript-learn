// union is a combination of two or three or more data types
// that you can put into a variable or an array
var score = 33;
score = 323;
score = "ast";
score = true;
var khamhit = { name: "asdf", id: 123 };
function getToken(id) {
  // making api call
  console.log("hello ggwp ".concat(id));
}
function getApi(id) {
  if (typeof id === "string") {
    id.toLowerCase();
  }
  id.toString();
}
